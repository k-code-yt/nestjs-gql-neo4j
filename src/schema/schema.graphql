### Inputs ###
input PersonInput {
  name: String
}
input SubscriberInput {
  email: String
}

### Types ###
type Person {
  id: String!
  name: String!
  title: String!
  age: Int!
}
type Subscriber {
  id: String!
  email: String!
}

### Queries ###
type Query {
  helloWorld: String
  getPerson: Person!
  getSubscriber: Subscriber!
}

### Mutations ###
type Mutation {
  addPerson(person: PersonInput): Person!
  addSubscriber(person: SubscriberInput): Subscriber!
}

### Subscriptions ###
type Subscription {
  trackAnyChange: Person
}
